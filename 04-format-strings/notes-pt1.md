~~~python
    slice1 = "Hello"
    slice2 = "World"

    contents = "p1={} p2={}"
    finalValue = contents.format(slice1, slice2)

    print(finalValue)

    """
        output
        p1=Hello p2=World
    """
~~~